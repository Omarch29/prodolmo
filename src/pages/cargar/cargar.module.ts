import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CargarPage } from './cargar';

@NgModule({
  declarations: [
    CargarPage,
  ],
  imports: [
    IonicPageModule.forChild(CargarPage),
  ],
})
export class CargarPageModule {}
